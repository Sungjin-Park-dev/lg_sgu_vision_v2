# 2026-04-15

## 한 일
- git 저장소 초기 커밋 (`66f545b`) — `scripts/`, `common/`, `docs/`, `ur20_description/`, 루트 설정 파일
- `.gitignore` 확장: `IsaacLab/`, `MotionComparator/`, `arm_coverage/`, `curobo/`, `data/`, `.claude/` 추가
- 날짜별 작업 로그 관습 시작 (`docs/logs/YYYY-MM-DD.md`)
- **docs 정리 & 재작성** (구현과 대조 후 최신화):
  - `clustering.md` 삭제 → `generate_viewpoints.md`로 병합 (Ward 알고리즘은 이미 구현에서 제거됨)
  - `visualize_viewpoints.md` 삭제 → `generate_viewpoints.md` 시각화 섹션으로 흡수
  - `zigzag-row-assignment-issue.md` → `docs/archive/`로 이동 (해결 완료)
  - `architecture.md` 재작성: Stage 2를 `plan_motion.py`로 교체 (prev 참조 제거), `publish_trajectory.py` 별도 Stage 3로 분리
  - `running.md` 재작성: 실제 CLI만 (`--publish`/`--dt`/`--num-rotations`은 존재하지 않음, `--cluster` 없음)
  - `config.md`: 실제 값으로 업데이트 (`ROBOT_START_STATE`, `TARGET_OBJECT.position`, `WALLS.support` 등), `DEFAULT_KNN`/`DEFAULT_LAMBDA_ROT`/`REPLAN_ENABLED` 추가, 미사용 `ROBOT_HAS_CONSTRAINT`/`MAX_JOINT_FROM_START_STATE` 제거
  - `CLAUDE.md`: 파이프라인 다이어그램 수정 (3단계 명시), 문서 링크 갱신
- **scripts 디렉토리 구조 정리** (기능별 4 폴더):
  - `scripts/pipeline/` — `generate_viewpoints`, `plan_motion`, `publish_trajectory` (핵심 3단계)
  - `scripts/ros2/` — `move_to_start`, `publish_workcell_markers`
  - `scripts/viz/` — `visualize_viewpoints` (내부 헬퍼), `visualize_coacd` (디버그)
  - `scripts/isaac/` — `ur_ros2_joint_control` (Isaac Sim용, `./python.sh` 실행)
  - `sys.path.insert(... parent.parent)` → `parent.parent.parent` 일괄 수정 (7개 파일)
  - import 경로 2곳 갱신: `scripts.visualize_viewpoints` → `scripts.viz.visualize_viewpoints`, `scripts.generate_viewpoints` → `scripts.pipeline.generate_viewpoints`
  - 각 스크립트 docstring 사용법과 docs 예시 경로 일괄 갱신
  - `CLAUDE.md` 디렉토리 구조 섹션 갱신 (`cuopt/` 제거 — 폴더 부재)

## 결정/변경
- **외부/대용량 폴더는 전부 ignore**: `IsaacLab` (1.2G), `MotionComparator` (1.3G), `arm_coverage` (6.5G), `curobo` (362M), `data` (88M)
- **`curobo/` 처리 보류**: CLAUDE.md 상 "수정된 포크"라 별도 repo 분리 또는 submodule 연결 필요 — 추후 결정
- **원격 repo 미연결**: 로컬 커밋만 우선 진행

## 이슈 / 다음 할 일
- [ ] `curobo/` fork 관리 방식 결정 (별도 repo? submodule?)
- [ ] 원격 repo (GitHub 등) 생성 후 `git remote add origin … && git push`
- [ ] `docs/logs/README.md` 인덱스 파일 필요 여부 검토
